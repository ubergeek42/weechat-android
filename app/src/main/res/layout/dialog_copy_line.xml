<?xml version="1.0" encoding="utf-8"?>
<!--
    this has a bit larger vertical padding to help separate multiline entries
    also has a max height of two lines with an ellipsis on the end
    -->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
          android:theme="@style/Theme.AppCompat.DayNight.Dialog.Alert"
          android:id="@+id/text"
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:paddingBottom="@dimen/dialog_item_padding_vertical"
          android:paddingTop="@dimen/dialog_item_padding_vertical"
          android:maxLines="2"
          android:ellipsize="end"
          android:singleLine="false"
          android:minHeight="?attr/listPreferredItemHeightSmall"
          android:textAppearance="?attr/textAppearanceListItemSmall"
          android:textColor="?attr/textColorAlertDialogListItem"
          android:gravity="center_vertical"
          android:paddingLeft="?attr/listPreferredItemPaddingLeft"
          android:paddingRight="?attr/listPreferredItemPaddingRight"
          android:background="?android:attr/selectableItemBackground" />